# Website Builder Manual Checklist

1) Create new site (restaurant, reservation enabled) -> AI generates content -> editor shows real content.
2) Edit hero headline -> save -> refresh -> still edited (persisted).
3) Regenerate About -> only about changes.
4) Publish -> public URL works -> chatbot loads.
5) Submit reservation -> stored in DB -> appears in dashboard reservations -> notification created.
6) Create non-restaurant site -> no reservation form -> contact form works.
7) Publish -> republish after edits -> public reflects changes.
